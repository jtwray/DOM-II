// const blocksNodeList = document.querySelectorAll(".block");
// const blocksList = Array.from(blocksNodeList);
// let pos=0;
// const launch=function LaunchRocket(block) {
// 	console.log(block);
// 	block.addEventListener("click", (ev) => {
// 		const id = setInterval(launch, 5);
// 		function launch() {
// 			pos == 350
				// ? clearInterval(id)
// 				: (pos++, (ev.target.style.top = `${pos}px`));
// 		}
// 	});
// }