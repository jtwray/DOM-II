// LaunchPad(block) {
// 	console.log(block);
// 	block.addEventListener("click", (ev) => {
// 		const id = setInterval(frame, 5);
// 		function frame() {
// 			pos == 350
				// ? clearInterval(id)
// 				: (pos++, (ev.target.style.left = `${pos}px`));
// 		}
// 	});
// }

// const down=() => {
//     clearInterval( id);
//     setInterval( returnToStart, 5 );
//     function returnToStart() {
//         pos=={100* parseInt(elemet.id) }? clearInterval( down ):( pos++, ( ev.target.style.down=`${pos}px` ) );
//     }
// }
// const drive=function addDrivetoLaunchPad(block) {
// 	console.log(block);
// 	block.addEventListener("click", (ev) => {
// 		const id = setInterval(up, 5);
// 		function up() {
//             pos==0
                // ? clearInterval( id )
// 				: (pos--, (ev.target.style.left = `${pos}px`));
// 		}
// 	});
// }

// blocksList.forEach(drive);
